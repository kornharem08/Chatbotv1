const func = require("../views/function.js");

   // setInterval มันจะทำให้ bot หลับ ไม่ได้ เราใช้ heroku ฟรี อยู่ มันควรจะได้หลับบ้างไม่งั้นประมาณสิ้นเดือน heroku มันจะหลับยาวเลย ถ้าตื่นเกิน 550 ชม.

    // setInterval(function() {
    //  func.sendTextMessage("2797221146971020","this is notification")
    // }, 60 * 60 * 1000);


